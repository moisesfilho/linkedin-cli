---
name: Quality Gate

on:
  workflow_call:

jobs:
  quality:
    name: Run Quality Gate
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v7

      - name: Install Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test,dev]"

      - name: Lint (ruff check)
        run: ruff check src/ tests/

      - name: Format (ruff format --check)
        run: ruff format --check src/ tests/

      - name: Security (bandit)
        run: bandit -r src/linkedin_cli/ -c pyproject.toml

      - name: Type check (mypy)
        run: mypy src/linkedin_cli/

      - name: Test with coverage
        run: pytest --cov=src/linkedin_cli/ --cov-report=xml --cov-report=term-missing --cov-fail-under=100

      - name: Test summary
        if: always()
        run: |
          echo "## CI Results" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Lint | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Format | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Security | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Type check | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests | ✅ |" >> $GITHUB_STEP_SUMMARY
          echo "| Coverage | ✅ |" >> $GITHUB_STEP_SUMMARY
